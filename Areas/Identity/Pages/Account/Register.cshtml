@page
@model StayGo.Areas.Identity.Pages.Account.RegisterModel

@{
    ViewData["Title"] = "Regístrate";
    Layout = "/Views/Shared/_Layout.cshtml";
}

@section Hero {
    <section class="hero">
        <div class="overlay">
            <div class="container d-flex align-items-center justify-content-center h-100">
                <div class="form-container" style="max-width:520px;">
                    <h2 class="text-white">Regístrate</h2>

                    <form id="registerForm" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="needs-validation" novalidate>
                        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

                        <div class="mb-3">
                            <label asp-for="Input.FirstName" class="form-label text-white">Nombre</label>
                            <input asp-for="Input.FirstName" class="form-control" required />
                            <span asp-validation-for="Input.FirstName" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Input.LastName" class="form-label text-white">Apellido</label>
                            <input asp-for="Input.LastName" class="form-control" required />
                            <span asp-validation-for="Input.LastName" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Input.Email" class="form-label text-white">Correo electrónico</label>
                            <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="nombre@ejemplo.com" required />
                            <span asp-validation-for="Input.Email" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Input.Password" class="form-label text-white">Contraseña</label>
                            <input asp-for="Input.Password" type="password" class="form-control" autocomplete="new-password" aria-required="true" required />
                            <span asp-validation-for="Input.Password" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Input.ConfirmPassword" class="form-label text-white">Confirma la contraseña</label>
                            <input asp-for="Input.ConfirmPassword" type="password" class="form-control" autocomplete="new-password" aria-required="true" required />
                            <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
                        </div>

                        <button id="registerSubmit" type="submit" class="w-100 btn btn-brand">Regístrate</button>
                    </form>

                    <!-- Divider -->
                    <div class="d-flex align-items-center gap-2 my-3 text-white-50">
                        <span class="flex-grow-1" style="height:1px;background:rgba(255,255,255,.35);"></span>
                        <span>o</span>
                        <span class="flex-grow-1" style="height:1px;background:rgba(255,255,255,.35);"></span>
                    </div>

                    <!-- Login externo (Google) -->
                    @if ((Model.ExternalLogins?.Count ?? 0) > 0)
                    {
                        <section class="text-center">
                            @await Html.PartialAsync("_ExternalLoginsPartial", Model.ExternalLogins)
                        </section>
                    }

                    <div class="text-center mt-3">
                        <span class="text-white-50">¿Ya tienes cuenta?</span>
                        <a class="ms-1" asp-page="./Login" asp-route-returnUrl="@Model.ReturnUrl">Inicia sesión</a>
                    </div>
                </div>
            </div>
        </div>
    </section>
}

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
